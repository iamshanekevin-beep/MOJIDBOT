{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "IQ_EMAIL",
      "description": "IQ Option account email (login credential for the trading bot)",
      "requiredAtBoot": true
    },
    {
      "name": "IQ_PASSWORD",
      "description": "IQ Option account password (login credential for the trading bot)",
      "requiredAtBoot": true
    },
    {
      "name": "TELEGRAM",
      "description": "Telegram bot token for notifications (optional)",
      "requiredAtBoot": false
    },
    {
      "name": "TELEGRAM_USER_ID",
      "description": "Telegram user/chat ID to receive notifications (optional)",
      "requiredAtBoot": false
    }
  ]
}
